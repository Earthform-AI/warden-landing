---
import { hero } from '../site.config';
import { EnhancedHeroSection } from './EnhancedHeroSection.tsx';
---

<EnhancedHeroSection 
  title={hero.title}
  subtitle={hero.subtitle}
  quote={hero.quote}
  cta={hero.cta}
  client:load 
/>

